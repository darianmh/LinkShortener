@model LinkShortener.Models.Link.ShowAllLinksModel<LinkShortener.Models.Link.AdminLinkModel>

@{
    Layout = "_MyLayout";
}


<div class="container Statics_container">
    <partial name="_LinkSortPartial" model="Model.SortModel" />
    <partial name="_LinkPagingPartial" model="Model.SortModel" />
    <hr />
    @{
        var count = 1;
    }
    @foreach (var item in Model.AllLinks)
    {
        <div class="row Statics_row">
            <div class="col3">
                <a target="_blank" href="@item.MainLink"><u>#@count</u>&nbsp;@item.MainLink</a>
            </div>
            <div class="col3">
                @{ var link = $"{item.ShortLink}%24";}
                <a target="_blank" href="@link"> @item.ShortLink</a>
            </div>
            <div class="col3">
                @item.VisitCount
            </div>
        </div>
        count++;
    }
    <hr />
    <partial name="_LinkPagingPartial" model="Model.SortModel" />
</div>